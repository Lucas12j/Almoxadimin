import 'package:flutter/material.dart';

class GiveBackItem extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text(
      "PÁGINA EM CONSTRUÇÃO 2",
      style: TextStyle(color: Colors.white),
    ));
  }
}
